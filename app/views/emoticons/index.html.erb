<ul class="emoticons">
  <% @emoticons.each do |e| %>
    <li>
      <a href="javascript:void(0)" title="<%= e.shortcut %>">
        <img src="<%= e.url %>" alt="<%= e.shortcut %>" title="<%= e.shortcut %>">
        <span>(<%= e.shortcut %>)</span>
      </a>
    </li>
  <% end %>
</ul>
